============
Installation
============

At the command line::

    $ pip install pointsToRfile

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pointsToRfile
    $ pip install pointsToRfile
